trigger AddRelatedRecord on Account(before insert,after insert, after update ,before delete,after delete ) {
  
      
    if(trigger.isbefore){
        if(trigger.isInsert){
           
            
          
        }
      
    }     
            
       
    
   
    
}